---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: 'latest'

run:
  dir: code-src
  path: ci/scripts/merge-pull-request

inputs:
- name: code-src
- name: build-resource


